{
  "name": "@open-device/website",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts && bun run serve.ts"
  },
  "dependencies": {
    "@open-device/core": "workspace:*",
    "@open-device/spec": "workspace:*"
  }
}
